profile: cover.out
threshold:
  total: 50
  package: 40
override:
  # cmd/blog is the wiring entrypoint (flags, http.Server, fsnotify loop) —
  # not unit-tested; covered by the local smoke + Pi deployment instead.
  - path: ^cmd/blog$
    threshold: 0
exclude:
  paths:
    - _test\.go$
